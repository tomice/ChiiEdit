ACLOCAL_AMFLAGS=-I m4
AUTOMAKE_OPTIONS = foreign
SUBDIRS = src

MAINTAINERCLEANFILES = aclocal.m4 compile \
        config.guess config.sub configure depcomp install-sh ltmain.sh \
        Makefile.in missing

EXTRA_DIST = \
        configure
